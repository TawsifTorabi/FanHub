<?php
// // PowerShell commands to be executed
// $commands = [
    // "[System.IO.Ports.SerialPort]::getportnames()",
    // //"\$port= new-Object System.IO.Ports.SerialPort COM7,115200,None,8,one; \$port.Open(); \$port.WriteLine('ping'); \$port.ReadLine(); \$port.Close();",
    // //"\$port1= new-Object System.IO.Ports.SerialPort COM3,115200,None,8,one; \$port1.Open(); \$port1.WriteLine('ping'); \$port1.ReadLine(); \$port1.Close();",
    // // '$port= new-Object System.IO.Ports.SerialPort COM7,115200,None,8,one',
    // // '$port.Open()',
    // // '$port.WriteLine("Hello world")',
    // // '$port.Close()',
    // // '$port= new-Object System.IO.Ports.SerialPort COM7,115200,None,8,one',
    // // '$port.Open()',
    // // '$port.ReadLine()'
// ];

// // Execute PowerShell commands and capture the output
// $output = '';
// foreach ($commands as $command) {
    // $output .= shell_exec('powershell.exe -command ' . escapeshellarg($command) . ' 2>&1') . "\n";
// }

// // Display the results in HTML
// echo "<pre>$output</pre>";
?>
